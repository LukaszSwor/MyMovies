import { createStore } from 'vuex';

// Utwórz nowy sklep Vuex za pomocą funkcji `createStore`
const store = createStore({
  state() {
    return {
      // stan początkowy
    };
  },
  mutations: {
    // mutacje
  },
  actions: {
    // akcje
  },
  getters: {
    // gettery
  }
});

export default store;
